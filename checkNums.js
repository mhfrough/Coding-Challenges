function CheckNums(num1, num2) {

    // code goes here
    if (num1 == num2) {
        return "-1"
    } else {
        return (num2 > num1)
    }

}

// keep this function call here 
console.log(CheckNums(readline()));